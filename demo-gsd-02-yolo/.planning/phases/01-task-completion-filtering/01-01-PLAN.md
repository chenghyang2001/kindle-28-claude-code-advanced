---
phase: 01-task-completion-filtering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app.py
  - test_app.py
autonomous: true
requirements:
  - TASK-01
  - TASK-02
  - TEST-01
  - TEST-02
  - TEST-03
must_haves:
  truths:
    - "complete_task(tasks, name) marks the matching task done=True and returns True; returns False when name not found"
    - "complete_task is idempotent: calling it on an already-done task returns True without error or side effect"
    - "list_pending(tasks) returns a list[str] of name values only for tasks where done=False"
    - "add_task, list_tasks, delete_task all function correctly with the new list[dict] storage model"
    - "All 11 pytest tests pass under PYTHONUTF8=1 pytest test_app.py -v (6 updated + 3 TestCompleteTask + 2 TestListPending)"
    - "Completed tasks remain deletable by delete_task (matches by name, ignores done state)"
  artifacts:
    - path: "app.py"
      provides: "complete_task and list_pending functions; dict-refactored add_task/delete_task/list_tasks"
      contains: "def complete_task"
    - path: "test_app.py"
      provides: "TestCompleteTask (3 tests) and TestListPending (2 tests), plus 6 updated existing tests"
      contains: "class TestCompleteTask"
  key_links:
    - from: "test_app.py"
      to: "app.py"
      via: "extended import line"
      pattern: "from app import.*complete_task.*list_pending"
---

<objective>
Refactor the existing brownfield CLI (app.py + test_app.py) from a list[str] task model to a list[dict] model, add complete_task and list_pending functions, and update all tests.

Purpose: Enable task completion tracking and pending filtering while keeping all existing behavior intact.
Output: app.py with 5 functions (dict model), test_app.py with 11 passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Current app.py — executor refactors these in place. Exact current signatures: -->

From app.py (current list[str] model to be replaced):
  add_task(tasks: list, name: str) -> None
    — strips name, raises ValueError if blank, appends cleaned string to tasks

  list_tasks(tasks: list) -> list
    — returns list(tasks), a shallow copy

  delete_task(tasks: list, name: str) -> bool
    — tasks.remove(name); returns True on success, catches ValueError and returns False

  main() -> None
    — REPL loop: "list" branch does: for index, task in enumerate(current, start=1): print(f"{index}. {task}")
    — NOTE: after dict refactor, must change {task} to {task['name']} to keep display clean

From test_app.py (current assertions use raw strings, must be updated to dict):
  TestAddTask.test_add_happy_path:    assert tasks == ["買牛奶"]
  TestListTasks.test_list_happy_path: tasks = ["a", "b"]; assert list_tasks(tasks) == ["a", "b"]
  TestListTasks.test_list_returns_copy: tasks = ["a"]; returned.append("b"); assert tasks == ["a"]
  TestDeleteTask.test_delete_happy_path: tasks = ["a", "b"]; assert tasks == ["b"] after delete
  TestDeleteTask.test_delete_non_existent_returns_false: tasks = ["a"]; assert tasks == ["a"] after failed delete
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor app.py to dict model and add complete_task / list_pending</name>
  <files>app.py</files>

  <read_first>
  Read app.py before editing. Key points extracted above in interfaces block.
  The module docstring currently says "task 以純字串 str 形式存放" — update it.
  The main() "list" branch prints {task} directly — must become {task['name']}.
  Do NOT add complete/pending REPL commands to main() — that is v2 scope.
  </read_first>

  <behavior>
  - add_task: tasks.append({"name": cleaned, "done": False}) instead of tasks.append(cleaned)
  - list_tasks: unchanged implementation (list(tasks) still works for list[dict])
  - delete_task: replace tasks.remove(name) with a for-loop searching task["name"] == name, pop by index, return True; return False if loop ends without match
  - complete_task: iterate tasks, find task["name"] == name, set task["done"] = True, return True; return False if not found (idempotent: already-done task still returns True)
  - list_pending: return [task["name"] for task in tasks if not task["done"]]
  - main() list branch: change print(f"{index}. {task}") to print(f"{index}. {task['name']}")
  </behavior>

  <action>
  Rewrite app.py in place. Update the module-level docstring to reflect list[dict] model and mention complete_task and list_pending. Keep the 5-function + main() structure.

  add_task: change the final append from tasks.append(cleaned) to tasks.append({"name": cleaned, "done": False}). Update type annotation comment in docstring from list[str] to list[dict].

  list_tasks: implementation stays as return list(tasks). Update docstring to note it returns a shallow copy of list[dict].

  delete_task: remove the try/except tasks.remove(name) block entirely. Replace with a for-loop using enumerate: for i, task in enumerate(tasks): if task["name"] == name: tasks.pop(i); return True. After the loop: return False. Update docstring param description.

  complete_task (new function, insert after delete_task): signature complete_task(tasks: list, name: str) -> bool. Iterate tasks; when task["name"] == name, set task["done"] = True and return True. After the loop return False. Docstring in Traditional Chinese with Args/Returns sections. Note idempotent behavior in docstring.

  list_pending (new function, insert after complete_task): signature list_pending(tasks: list) -> list. Return a list comprehension [task["name"] for task in tasks if not task["done"]]. Docstring in Traditional Chinese with Args/Returns. Note it returns name strings, not dicts.

  main(): in the "list" branch, change the inner print from print(f"{index}. {task}") to print(f"{index}. {task['name']}"). No other changes to main(); do not add complete or pending REPL commands.

  All docstrings must be Traditional Chinese, Google-style (Args:/Returns:/Raises: sections). 4-space indentation. No bare except. No new imports needed.
  </action>

  <verify>
    <automated>PYTHONUTF8=1 python -c "import app; t=[]; app.add_task(t,'買牛奶'); assert t==[{'name':'買牛奶','done':False}]; app.complete_task(t,'買牛奶'); assert t[0]['done'] is True; assert app.list_pending(t)==[]; app.delete_task(t,'買牛奶'); assert t==[]"</automated>
  </verify>

  <acceptance_criteria>

- add_task(tasks, "  買牛奶  ") results in tasks == [{"name": "買牛奶", "done": False}]
- complete_task(tasks, "買牛奶") returns True and tasks[0]["done"] is True
- complete_task called again on same task returns True (idempotent)
- complete_task(tasks, "不存在") returns False
- list_pending returns only names of tasks where done=False
- delete_task works by name regardless of done state
- Module imports cleanly: python -c "import app" produces no error
  </acceptance_criteria>

  <done>app.py contains 5 functions (add_task, list_tasks, delete_task, complete_task, list_pending) + main(), all operating on list[dict] with {"name": str, "done": bool} schema. Inline smoke test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update test_app.py — update 6 existing tests to dict model, add TestCompleteTask and TestListPending</name>
  <files>test_app.py</files>

  <read_first>
  Read test_app.py before editing. Current 6 tests use raw strings (tasks = ["a", "b"] etc.) — all 6 must be updated to dict form. Add complete_task and list_pending to the import line. Do NOT add TestMain or any test for main().
  </read_first>

  <behavior>
  TestAddTask.test_add_happy_path: assert tasks == [{"name": "買牛奶", "done": False}]
  TestAddTask.test_add_empty_name_raises: assert tasks == [] (unchanged — empty list equality holds)
  TestListTasks.test_list_happy_path: tasks = [{"name": "a", "done": False}, {"name": "b", "done": False}]; assert [t["name"] for t in list_tasks(tasks)] == ["a", "b"]
  TestListTasks.test_list_returns_copy: tasks = [{"name": "a", "done": False}]; returned.append({"name": "b", "done": False}); assert len(tasks) == 1
  TestDeleteTask.test_delete_happy_path: tasks = [{"name": "a", "done": False}, {"name": "b", "done": False}]; assert tasks == [{"name": "b", "done": False}] after delete
  TestDeleteTask.test_delete_non_existent_returns_false: tasks = [{"name": "a", "done": False}]; assert tasks == [{"name": "a", "done": False}] after failed delete
  TestCompleteTask.test_complete_happy_path: tasks = [{"name": "a", "done": False}]; complete_task returns True; tasks[0]["done"] is True
  TestCompleteTask.test_complete_not_found_returns_false: tasks = [{"name": "a", "done": False}]; complete_task(tasks, "x") is False; tasks unchanged
  TestCompleteTask.test_complete_idempotent: tasks = [{"name": "a", "done": True}]; complete_task(tasks, "a") is True; tasks[0]["done"] is True
  TestListPending.test_list_pending_all_pending: tasks = 2 not-done dicts; list_pending returns ["a", "b"]
  TestListPending.test_list_pending_some_done: tasks = one done + one pending; list_pending returns only pending name
  </behavior>

  <action>
  Edit test_app.py in place. Keep the module docstring; update it to mention the dict model and new test classes.

  Import line: change "from app import add_task, delete_task, list_tasks" to "from app import add_task, complete_task, delete_task, list_pending, list_tasks" (alphabetical order, consistent with existing style).

  TestAddTask.test_add_happy_path: change assertion from assert tasks == ["買牛奶"] to assert tasks == [{"name": "買牛奶", "done": False}].

  TestAddTask.test_add_empty_name_raises: assert tasks == [] stays unchanged (empty list).

  TestListTasks.test_list_happy_path: replace tasks = ["a", "b"] with tasks = [{"name": "a", "done": False}, {"name": "b", "done": False}]. Change assertion to assert [t["name"] for t in list_tasks(tasks)] == ["a", "b"]. Update Traditional Chinese docstring to match.

  TestListTasks.test_list_returns_copy: replace tasks = ["a"] with tasks = [{"name": "a", "done": False}]. Change returned.append("b") to returned.append({"name": "b", "done": False}). Change final assertion to assert len(tasks) == 1.

  TestDeleteTask.test_delete_happy_path: replace tasks = ["a", "b"] with dict list. Change final assertion to assert tasks == [{"name": "b", "done": False}].

  TestDeleteTask.test_delete_non_existent_returns_false: replace tasks = ["a"] with dict form. Change final assertion to assert tasks == [{"name": "a", "done": False}].

  Add class TestCompleteTask after TestDeleteTask with class docstring "complete_task 的行為測試。" and three methods:
    test_complete_happy_path: docstring "存在的任務標記為完成應回傳 True 且 done 為 True。"; create tasks with one not-done dict; assert complete_task returns True; assert tasks[0]["done"] is True.
    test_complete_not_found_returns_false: docstring "不存在的任務應回傳 False 且清單不變。"; create tasks with one not-done dict; assert complete_task(tasks, "x") is False; assert tasks == [{"name": "a", "done": False}].
    test_complete_idempotent: docstring "已完成任務再次標記應仍回傳 True（冪等性）。"; create tasks = [{"name": "a", "done": True}]; assert complete_task(tasks, "a") is True; assert tasks[0]["done"] is True.

  Add class TestListPending after TestCompleteTask with class docstring "list_pending 的行為測試。" and two methods:
    test_list_pending_all_pending: docstring "全部未完成時，所有任務名稱應出現於回傳清單。"; tasks = two not-done dicts ("a", "b"); assert list_pending(tasks) == ["a", "b"].
    test_list_pending_some_done: docstring "部分已完成時，只有未完成任務的名稱出現。"; tasks = [{"name": "a", "done": True}, {"name": "b", "done": False}]; assert list_pending(tasks) == ["b"].

  All test method docstrings must be Traditional Chinese. Each test method creates its own inline tasks list — no shared fixtures or setUp.
  </action>

  <verify>
    <automated>PYTHONUTF8=1 pytest test_app.py -v</automated>
  </verify>

  <acceptance_criteria>

- PYTHONUTF8=1 pytest test_app.py -v shows 11 passed, 0 failed
- TestAddTask (2 tests), TestListTasks (2 tests), TestDeleteTask (2 tests) all green
- TestCompleteTask (3 tests) all green: happy path, not found, idempotent
- TestListPending (2 tests) all green: all pending, some done
- No test references raw string tasks (tasks = ["a"]) — all use dict model
  </acceptance_criteria>

  <done>test_app.py has 11 tests across 5 classes; PYTHONUTF8=1 pytest test_app.py -v reports 11 passed.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Pure in-memory Python CLI; no network, no auth, no file I/O, no external services |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | task name input in add_task | accept | name.strip() + ValueError on blank already present; no SQL/shell injection surface |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan; only pytest (already in requirements.txt) is used |
</threat_model>

<verification>
Final check after both tasks complete:

1. `PYTHONUTF8=1 pytest test_app.py -v` — must show 11 passed, 0 failed
2. `PYTHONUTF8=1 python -c "import app; print('OK')"` — no import errors
3. Confirm test_app.py import line includes complete_task and list_pending
4. Confirm no task in test_app.py uses a raw string list (no tasks = ["a", "b"] pattern)
</verification>

<success_criteria>

- app.py: 5 functions using list[dict] model + main() with {task['name']} display
- test_app.py: 11 tests across 5 classes, all green under PYTHONUTF8=1 pytest test_app.py -v
- All 5 requirement IDs covered: TASK-01 (complete_task), TASK-02 (list_pending), TEST-01 (TestCompleteTask 3 tests), TEST-02 (TestListPending 2 tests), TEST-03 (existing 6 updated and green)
- No REPL wiring for complete/pending (v2 deferred per CONTEXT.md)
- No new external dependencies introduced
</success_criteria>

<output>
Create `.planning/phases/01-task-completion-filtering/01-01-SUMMARY.md` when done.
</output>
