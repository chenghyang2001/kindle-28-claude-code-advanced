---
phase: 01-status
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app.py
  - test_app.py
autonomous: true
requirements: [STAT-01, STAT-02, STAT-03, STAT-04]

must_haves:
  truths:
    - "add_task 新增的 task 是 dict{name, done=False}，name 已 strip，空白/None 仍拋 ValueError"
    - "list_tasks 仍回傳防禦性淺 copy（改動回傳值不動到原 list）"
    - "delete_task 以 task['name'] 比對，刪到回 True、找不到回 False，不拋例外"
    - "complete_task(tasks, name) 把第一個相符 task 的 done 設為 True 回 True；找不到回 False；已完成再標記仍回 True（idempotent）；比對前 strip"
    - "list_pending(tasks) 只回 done=False 的 task 的淺 copy，維持原插入順序，空/全完成回 []"
    - "main() 列表顯示讀 task['name']，dict 任務仍可正確列印"
    - "既有 6 個 pytest 測試（已對齊 dict 模型）維持全綠"
    - "新增 complete_task / list_pending 的 pytest 測試（happy/edge/error）全綠"
  artifacts:
    - path: "app.py"
      provides: "dict 模型遷移 + add/list/delete/complete/pending + main 顯示相容"
      contains: "def complete_task"
    - path: "app.py"
      provides: "list_pending 查詢函式"
      contains: "def list_pending"
    - path: "test_app.py"
      provides: "對齊 dict 模型的既有測試 + Status 新測試"
      contains: "class TestCompleteTask"
  key_links:
    - from: "main()"
      to: "task['name']"
      via: "list 顯示迴圈讀字典 name 欄位"
      pattern: "task\\[.name.\\]"
    - from: "complete_task / delete_task"
      to: "name.strip()"
      via: "比對前統一 strip"
      pattern: "\\.strip\\(\\)"
---

<objective>
把既有 todo-CLI 的 task 從純字串 `str` 升級為 `dict{name, done}`，新增「標記完成」
（`complete_task`）與「只看未完成」（`list_pending`）兩個查詢能力，且讓既有
`add_task` / `list_tasks` / `delete_task` 的對外行為與 `main()` CLI 顯示在新資料結構下
維持正確。

Purpose: 為 Phase 2 的持久化（`save_tasks` 要存 `done` 欄位）奠定資料結構基礎；同時
交付使用者「標記任務完成 + 只檢視未完成任務」的能力。
Output: 修改後的 `app.py`（5 函式 + main 顯示相容）與 `test_app.py`（既有 6 測試對齊 dict
模型 + 新增 Status 測試）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<phase_goal>
**As a** todo-CLI 使用者, **I want to** 把任務標記為完成並只檢視尚未完成的任務,
**so that** 我能追蹤待辦進度而不被已完成項目干擾。

> 注意：本階段聚焦「函式層」能力（STAT-01..04）。把新函式接上 CLI 指令（`complete` /
> `pending`）屬 v2 deferred（見 01-CONTEXT.md `## Deferred Ideas`），本計畫不得實作。
</phase_goal>

<constraints>
- 測試執行方式：`PYTHONUTF8=1 pytest test_app.py -v`（Windows + cp950 環境，必須加 PYTHONUTF8=1）
- 僅用 Python 標準庫，**不得新增任何依賴**（requirements.txt 維持只有 pytest）
- task dict 欄位固定為兩個：`name`（str）與 `done`（bool），對齊 PROJECT.md / 01-CONTEXT.md
- 不改 CLI loop 的指令集（不加 complete/pending 指令）、不做持久化（Phase 2）
- 風格須與既有 app.py 一致：純函式、繁體中文 docstring、ValueError（空輸入）+ 布林回傳（找不到）
</constraints>

<context>
@.planning/phases/01-status/01-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@app.py
@test_app.py

<interfaces>
<!-- 既有契約（執行者直接沿用，無需再探索 codebase）。遷移後新形態如下。 -->

app.py 函式契約（遷移後目標）：

- add_task(tasks: list, name: str) -> None   # 變更：append dict{"name": <stripped>, "done": False}
- list_tasks(tasks: list) -> list            # 不變：回 list(tasks) 淺 copy
- delete_task(tasks: list, name: str) -> bool # 變更：以 task["name"] == name.strip() 比對後移除
- complete_task(tasks: list, name: str) -> bool  # 新增
- list_pending(tasks: list) -> list              # 新增
- main() -> None                              # 變更：list 顯示讀 task["name"]

task 形態：{"name": str, "done": bool}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 把 task 遷移為 dict 模型並修正既有函式與 main 顯示</name>
  <files>app.py, test_app.py</files>
  <behavior>
    - add_task：傳入 "  買牛奶  " → tasks == [{"name": "買牛奶", "done": False}]
    - add_task：傳入 "" 或 "   " 或 None → 拋 ValueError（行為不變）
    - list_tasks：回傳值 append 後原 tasks 不變（淺 copy 不變）
    - delete_task：tasks 內含 {"name": "買牛奶", ...} 時 delete_task(tasks, " 買牛奶 ") → True 且移除該 dict
    - delete_task：找不到 name → 回 False、tasks 不變
    - main：list 指令對 dict 任務印出 "1. 買牛奶"（只顯示 name，不印整個 dict）
  </behavior>
  <action>
實作 STAT-01（task 改為 dict{name, done}、既有 add/list/delete 對外行為不變）。
(a) `add_task`：保留 None/空白 strip 與 ValueError 檢查，但把 `tasks.append(name.strip())`
    改為 append `{"name": name.strip(), "done": False}`（per 01-CONTEXT「資料結構與遷移」）。
(b) `delete_task`：原本 `if target in tasks: tasks.remove(target)` 對 dict 永遠 False，必須改寫——
    先 `target = name.strip() if name else name`，再以迴圈/next 找出第一個 `task["name"] == target`
    的元素並 `tasks.remove(該元素)`，刪到回 True、找不到回 False（不拋例外）。
(c) `list_tasks`：維持 `return list(tasks)` 淺 copy，不需改動（dict 元素同樣以 reference 共享，
    符合「淺 copy 已足夠、呼叫端不應原地改」的既定設計）。
(d) `main()`：list 顯示迴圈把 `print(f"{index}. {task}")` 改為 `print(f"{index}. {task['name']}")`，
    讓 dict 任務只顯示 name（done 顯示屬 v2 範疇，本階段最小變更維持可列印）。add/delete 分支
    顯示用的是 `arg.strip()`，不讀 task 結構，無需改動。
(e) 更新既有 6 個測試使其對齊 dict 模型，**不可弱化原意**：
    - `test_add_happy_path`：assert 改為 `tasks == [{"name": "買牛奶", "done": False}]`（仍驗證 strip + 預設未完成）。
    - `test_add_empty_name_raises`：不變（仍驗 ValueError）。
    - `test_list_happy_path` / `test_list_returns_copy`：fixtures 改用 dict（如
      `[{"name": "a", "done": False}, {"name": "b", "done": False}]`），仍驗證內容一致與 copy 行為。
    - `test_delete_happy_path` / `test_delete_non_existent_returns_false`：fixtures 改用 dict，
      仍驗證刪到回 True/移除、找不到回 False/不變。
不得使用「v1 / 簡化版 / 暫時 hardcode」等縮水寫法。
  </action>
  <verify>
    <automated>cd "C:/Users/B00332/workspace/kindle-28-claude-code-advanced/demo-gsd-03-yolo-2-phases" && PYTHONUTF8=1 python -m pytest test_app.py -v</automated>
  </verify>
  <done>遷移後既有 6 個測試（已對齊 dict）全綠；add_task 產生 dict{name, done=False}；delete_task 以 name 比對；main list 顯示只印 name。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 新增 complete_task 與 list_pending</name>
  <files>app.py</files>
  <behavior>
    - complete_task：tasks 含未完成的 "買牛奶" → complete_task(tasks, "買牛奶") 回 True 且該 task done=True
    - complete_task：name 前後有空白（" 買牛奶 "）仍能比對成功
    - complete_task：找不到 name → 回 False，tasks 不變
    - complete_task：對已 done=True 的 task 再次標記 → 仍回 True、done 維持 True（idempotent）
    - complete_task：多個同名 task 只標記「第一個」相符者
    - list_pending：混合 done True/False → 只回 done=False 的 dict，維持原插入順序
    - list_pending：空清單或全部完成 → 回 []
    - list_pending：回傳為淺 copy（append 回傳值不改動原 tasks 長度）
  </behavior>
  <action>
實作 STAT-02 與 STAT-03，與既有純函式風格一致（傳入 tasks、無 I/O、繁中 docstring）。
- `complete_task(tasks, name) -> bool`（per 01-CONTEXT「complete_task 行為」）：比對前對 name 做
  `name.strip()`（與 add/delete 一致）；找出第一個 `task["name"] == 目標` 的 task 設 `task["done"] = True`
  並回 True；找不到回 False（不拋例外，與 delete_task 一致）；已完成再標記維持 done=True 並回 True
  （idempotent）；只處理第一個相符者。
- `list_pending(tasks) -> list`（per 01-CONTEXT「list_pending 行為」）：回傳由 `done=False` 的 task
  組成的新 list（淺 copy，元素為原 dict reference），維持原始插入順序；空清單或全部完成回 `[]`。
  以 list comprehension `[t for t in tasks if not t["done"]]` 實作即可自然保序。
建議把這兩個函式放在 delete_task 之後、main 之前，與既有三函式並列。
  </action>
  <verify>
    <automated>cd "C:/Users/B00332/workspace/kindle-28-claude-code-advanced/demo-gsd-03-yolo-2-phases" && PYTHONUTF8=1 python -c "from app import complete_task, list_pending; t=[{'name':'a','done':False},{'name':'b','done':True}]; print(complete_task(t,' a '), t[0]['done'], complete_task(t,'x'), [x['name'] for x in list_pending(t)])"</automated>
  </verify>
  <done>complete_task / list_pending 已加入 app.py 並 importable；smoke 檢查輸出 `True True False []`（a 被標記完成後 pending 為空）。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 新增 complete_task / list_pending 的 pytest 測試（happy/edge/error）</name>
  <files>test_app.py</files>
  <behavior>
    - TestCompleteTask.test_complete_happy_path：標記未完成 task → 回 True 且 done=True
    - TestCompleteTask.test_complete_strips_name：name 帶空白仍比對成功
    - TestCompleteTask.test_complete_idempotent：對已完成 task 再標記 → 回 True、done 維持 True
    - TestCompleteTask.test_complete_not_found_returns_false：找不到 → 回 False、tasks 不變
    - TestListPending.test_pending_happy_path：混合狀態只回 done=False 並保序
    - TestListPending.test_pending_all_done_returns_empty：全部完成 → []
    - TestListPending.test_pending_returns_copy：回傳為淺 copy，append 不動到原 tasks
  </behavior>
  <action>
實作 STAT-04 的新增測試部分。在 test_app.py 新增 `class TestCompleteTask` 與 `class TestListPending`，
沿用既有檔案的 import 與測試風格（`Test<Subject>` 類別、`test_<behavior>` 方法、繁中 docstring）。
更新 import 行加入 `complete_task, list_pending`。涵蓋至少：
- complete_task：happy（標記成功回 True + done 變 True）、strip（name 帶空白仍成功）、
  idempotent（已完成再標記回 True 且 done 不變）、error/edge（找不到回 False 且 tasks 不變）。
- list_pending：happy（混合狀態只回未完成且保序）、edge（全部完成或空清單回 []）、
  copy（回傳 append 後原 tasks 不變）。
fixtures 一律用 dict{name, done} 形態。不得新增依賴，不得弱化斷言（要斷言實際 done 值與順序，
不可只斷言長度）。
  </action>
  <verify>
    <automated>cd "C:/Users/B00332/workspace/kindle-28-claude-code-advanced/demo-gsd-03-yolo-2-phases" && PYTHONUTF8=1 python -m pytest test_app.py -v</automated>
  </verify>
  <done>全部測試（既有 6 個 + 新增 complete_task/list_pending 測試）綠燈；新測試涵蓋 happy/edge/error。</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI stdin → core functions | 使用者於 REPL 輸入的字串經 main() 解析後傳入純函式 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | add_task 輸入名稱 | mitigate | 既有 strip + ValueError 空白驗證沿用；dict 僅含 name/done 兩固定欄位，不接受任意鍵 |
| T-01-02 | Denial of Service | list_pending / complete_task 線性掃描 | accept | 單機單人 in-memory 清單，資料量極小，O(n) 掃描無實質風險 |
| T-01-03 | Information Disclosure | 無持久化、無網路、無 PII | accept | 本階段純記憶體；無檔案/DB/網路輸出（持久化屬 Phase 2） |
| T-01-SC | Tampering | pip 套件安裝 | mitigate | 本計畫不安裝任何套件（僅標準庫 + 既有 pytest）；無套件合法性風險 |
</threat_model>

<verification>
- `cd <project root> && PYTHONUTF8=1 python -m pytest test_app.py -v` → 全綠（既有 6 + 新增）
- `PYTHONUTF8=1 python -c "import app; t=[]; app.add_task(t,'x'); assert t==[{'name':'x','done':False}]"` → 無錯
- `git grep -n "task\['name'\]" app.py` → main() 顯示已讀 name 欄位
- requirements.txt 仍只有 pytest（無新增依賴）
</verification>

<success_criteria>

- ROADMAP Phase 1 五項 Success Criteria 全部為真：
  1. add/list/delete 對外行為不變（ValueError / 防禦性 copy / True|False）— STAT-01
  2. complete_task 可把指定任務 done 設 True — STAT-02
  3. list_pending 只取得 done=False 任務 — STAT-03
  4. 既有 6 個 pytest 全綠 — STAT-04
  5. 新增 complete_task/list_pending 測試（happy/edge/error）全綠 — STAT-04
- task dict 欄位固定為 name 與 done；無新增依賴；CLI 指令集未變（v2 deferred 未實作）
</success_criteria>

<output>
Create `.planning/phases/01-status/01-01-SUMMARY.md` when done
</output>
