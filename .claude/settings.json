{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "git -C \"$CLAUDE_PROJECT_DIR\" log -1 --format=\"📚 學習進度：%s (%h)\""
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(python -c \"import json,sys;print(json.load(sys.stdin).get('tool_input',{}).get('file_path',''))\" 2>/dev/null); case \"$f\" in *.py) command -v ruff >/dev/null 2>&1 && { ruff check --fix \"$f\" >/dev/null 2>&1; ruff format \"$f\" >/dev/null 2>&1; echo \"🐍 ruff 已整理：$f\"; } ;; esac"
          }
        ]
      }
    ]
  }
}
